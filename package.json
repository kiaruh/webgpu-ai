{
    "name": "immersive-intelligence-platform",
    "version": "0.0.1",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev:client": "npm run dev --workspace=@immersive/client",
        "dev:server": "npm run start:dev --workspace=@immersive/server",
        "build": "npm run build --workspaces",
        "lint": "npm run lint --workspaces"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}